# How to Test Notion API

-   Create `.env` file inside root directory and add your notion API keys. (Use `.env.example` as a reference)

-   Install dependencies and run:

    ```node
    npm run start
    ```

    to test the API response.
